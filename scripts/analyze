#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
PYTHON_BIN="${PYTHON_BIN:-.venv/bin/python}"
if [ ! -x "$PYTHON_BIN" ]; then
  PYTHON_BIN="${PYTHON:-python3}"
fi
PYTHONPATH="${PYTHONPATH:-src}" exec "$PYTHON_BIN" -m listing_agent.cli analyze "$@"
